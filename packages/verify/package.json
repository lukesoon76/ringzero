{
  "name": "@ring-zero/verify",
  "version": "0.0.0",
  "private": true,
  "description": "Python logical-verification subsystem (uv-managed). Run via `pnpm verify:py`.",
  "scripts": {
    "check": "uv run ruff check . && uv run mypy src && uv run pytest"
  }
}
