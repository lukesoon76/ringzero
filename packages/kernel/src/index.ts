/**
 * @ring-zero/kernel ★ REAL (deep) — Pillar P4: Runtime Execution Governance.
 *
 * The deterministic execution-governance kernel: the guard engine, the labelled
 * transition system W = (s0, S, Π, δ), action selection a(s,Θ), and tiered
 * containment. Pure, deterministic, bounded-time — NO LLM on the binding path.
 *
 * Phase 0: stub only. The canonical objects this package will implement are
 * defined in ../../ARCHITECTURE.md. Built for real in Phase 1.
 */

export const PACKAGE = "@ring-zero/kernel";
export const PHASE = 0;

/** Build stance for this pillar, surfaced in the console pillar map. */
export const STANCE = "REAL" as const;
