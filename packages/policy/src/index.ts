/**
 * @ring-zero/policy — P4: capability catalogue + typed builder DSL compiling to the transition system W; C1-C4 definitions; tier-to-control map.
 *
 * Phase 0: stub only. See ../../ARCHITECTURE.md and ../../CLAUDE.md.
 */

export const PACKAGE = "@ring-zero/policy";
export const PHASE = 0;

/** Build stance for this pillar, surfaced in the console pillar map. */
export const STANCE = "REAL" as const;
